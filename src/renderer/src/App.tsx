export const App = (): JSX.Element => {
  return (
    <main className="app-shell">
      <section className="empty-state">
        <p className="eyebrow">githubg</p>
        <h1>GitHub PRs, calmly gathered.</h1>
        <p>Electron, React, and TypeScript are wired up and ready for the app layers.</p>
      </section>
    </main>
  );
};
